# Copy to tests/Integration/.env (or export in your shell) and fill in DHL Express
# sandbox credentials. .env files in this directory are gitignored.
#
# Sandbox quota: 500 calls / day per credential set — keep test runs deliberate.

DHL_EXPRESS_USERNAME=
DHL_EXPRESS_PASSWORD=
DHL_EXPRESS_ACCOUNT_NUMBER=
